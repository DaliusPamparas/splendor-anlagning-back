'use strict';

/**
 * checklist-completion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::checklist-completion.checklist-completion');